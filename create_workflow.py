#!/usr/bin/env python3
"""创建文档驱动5阶段工作流模板"""
import sys, json, os
sys.path.insert(0, 'src')

from template_registry import TemplateRegistry
from migration.scripts import run_migration

db = "/home/administrator/.hermes/state/workflows.db"
run_migration(db_path=db, dry_run=False)

reg = TemplateRegistry(db_path=db)

PROMPT_S1 = """【做什么】根据任务类型（技术方案/产品需求/测试用例/接口文档/运维手册）生成对应的结构化文档
【怎么做】
1. 分析用户原始需求，提取显性需求和隐性需求
2. 按照文档模板生成结构化文档，必须包含以下章节：
   - 背景与目标：说明为什么要做这件事，期望达到什么效果
   - 范围与边界：明确做什么、不做什么、影响范围
   - 详细设计/步骤/用例：具体的实现方案、步骤或测试用例
   - 风险与依赖：识别技术风险、依赖项、降级方案
   - 验收标准：可量化、可测试的验收条件
3. 文档必须可执行，禁止写"待定"、"TODO"、"后续补充"等占位符
4. 量化指标必须具体，禁止写"高性能"，必须写"P99延迟<200ms，支持QPS≥5000"
【验收标准】
1. 文档包含全部5个章节（背景与目标、范围与边界、详细设计、风险与依赖、验收标准）
2. 所有验收标准可量化、可测试
3. 文档格式符合对应类型模板
4. 无占位符或模糊表述"""

PROMPT_S2 = """【做什么】使用Codex对阶段1产出的文档进行三方20维度审查
【怎么做】
1. 运行 codex review 命令审查文档
2. 审查20个维度：逻辑一致性、完整性、清晰度、可执行性、安全性、性能、可维护性、可测试性、向后兼容、错误处理、并发安全、数据完整性、API设计、文档质量、代码影响、回滚方案、可观测性、认证模型、协议合规、原子性
3. 结论必须原文保留，不可篡改、删减或曲解
4. 凡是Codex标记为Issue的条目，原则上必须修复
5. 若认定某条为误报，必须根据该问题修改文档对应内容，重新提交Codex全量审查（非定向复测），需连续两轮该问题不再出现，方可视为通过
6. 连续两轮全维度审查均为0 Issue，方可通过
【验收标准】
1. 连续两轮Codex审查均为0 Issue
2. 每轮审查结论完整保留
3. 所有Issue已修复或确认为误报（经连续两轮验证）"""

PROMPT_S3 = """【做什么】逐条对照用户原始需求，验证文档是否完整覆盖所有显性需求与隐性隐含诉求
【怎么做】
1. 获取用户原始需求/问题清单
2. 逐条对照文档内容，检查每个需求是否被覆盖
3. 识别隐性需求（用户未明确说但逻辑上必须有的需求）
4. 对于未覆盖的需求，标记为缺失并说明影响
5. 若存在缺失，需回退至阶段1补充文档，并重新进入阶段2审查
【验收标准】
1. 所有显性需求逐条对应到文档章节
2. 隐性需求已识别并覆盖
3. 无遗漏的需求项
4. 需求追溯矩阵完整"""

PROMPT_S4 = """【做什么】基于已通过审查的文档进行技术设计或开发/架构任务书设计
【怎么做】
1. 4-A 架构设计（架构师/技术负责人）：
   - C4模型：至少提供Level 1（系统上下文）和Level 2（容器/服务）的架构图描述
   - ADR格式：标题、背景、决策、后果、日期、状态
   - 量化指标：禁止写"高性能"，必须写"P99延迟<200ms，支持QPS≥5000"
   - 模块/服务划分需含职责边界与依赖关系
   - 数据模型与存储选型理由
   - 部署架构与容灾/扩缩容设计
2. 4-B 详细设计（开发工程师）：
   - 接口契约：使用OpenAPI 3.0/Proto文件/Thrift IDL等契约式描述，禁止纯自然语言
   - 异常覆盖：明确写出哪些异常可重试、哪些不可重试、重试次数和退避策略
   - 自测清单：至少覆盖正常路径、边界条件、异常路径三类
3. 设计不得超出文档约定范围，如有变更需走变更流程并重审
【验收标准】
1. 架构图完整（Level 1 + Level 2）
2. ADR记录完整（标题、背景、决策、后果、日期、状态）
3. 量化指标明确具体
4. 接口契约使用契约式描述
5. 异常处理策略完整
6. 自测清单覆盖三类路径"""

PROMPT_S5 = """【做什么】对照最终版文档，逐项检查实现结果（代码/配置/测试报告等）是否与文档100%一致
【怎么做】
1. 获取最终版文档
2. 逐项检查实现结果：
   - 代码实现是否与设计文档一致
   - 配置是否与文档描述一致
   - 测试报告是否覆盖文档中的验收标准
3. 标记所有差异项
4. 检查是否有新增未文档化的内容
5. 差异项需修复后回归检查
【验收标准】
1. 实现与文档100%一致
2. 无新增未文档化内容
3. 所有差异项已修复
4. 一致性检查报告完整"""

APPROVE_S1 = """请审查文档完整性：
1. 背景与目标是否清晰说明了为什么要做这件事
2. 范围与边界是否明确界定了做什么和不做什么
3. 详细设计/步骤/用例是否足够具体可执行
4. 风险与依赖是否已识别并给出缓解措施
5. 验收标准是否可量化、可测试
6. 文档中是否有占位符或模糊表述
7. 量化指标是否具体（禁止"高性能"等模糊词）"""

APPROVE_S2 = """请确认Codex审查结果：
1. 是否连续两轮0 Issue
2. 每轮审查结论是否完整保留
3. 所有Issue是否已修复
4. 误报是否经过连续两轮验证"""

APPROVE_S3 = """请验证需求覆盖：
1. 显性需求是否逐条对应到文档
2. 隐性需求是否已识别并覆盖
3. 是否存在遗漏的需求项
4. 需求追溯矩阵是否完整"""

APPROVE_S4 = """请审查设计文档：
1. 架构图是否完整（Level 1 + Level 2）
2. ADR记录是否完整（标题、背景、决策、后果、日期、状态）
3. 量化指标是否明确具体
4. 接口契约是否使用契约式描述
5. 异常处理策略是否完整
6. 自测清单是否覆盖三类路径"""

APPROVE_S5 = """请确认一致性检查结果：
1. 实现与文档是否100%一致
2. 是否有新增未文档化内容
3. 所有差异项是否已修复
4. 一致性检查报告是否完整"""

reg.register({
    "workflow_id": "WL-09",
    "name": "文档驱动5阶段工作流",
    "description": "完整的文档驱动开发流程：文档产出→Codex审查→需求对齐→开发落地→一致性检查。适用于新功能开发、架构改造、技术方案评审等场景。",
    "trigger_scene": ["新功能开发任务", "架构改造任务", "技术方案评审任务"],
    "allowed_initiators": ["pm", "coordinator", "lr"],
    "allowed_executors": ["pm", "product_architect", "engineer", "reviewer", "qa"],
    "max_duration_hours": 168,
    "quality_standards": "所有阶段通过Codex审查，实现与文档100%一致，无新增未文档化内容",
    "steps": [
        {
            "step_id": "s1",
            "title": "阶段1: 文档产出",
            "type": "handoff",
            "target_role": "product_architect",
            "prompt_template": PROMPT_S1,
            "approval_prompt": APPROVE_S1,
            "failure_patterns": ["文档缺少关键章节", "验收标准不可测", "存在占位符或模糊表述", "量化指标不具体"],
        },
        {
            "step_id": "s2",
            "title": "阶段2: Codex三方审查",
            "type": "gate",
            "target_role": "reviewer",
            "prompt_template": PROMPT_S2,
            "approval_prompt": APPROVE_S2,
            "failure_patterns": ["Codex发现Issue", "未连续两轮通过", "审查结论被篡改", "Issue未修复"],
        },
        {
            "step_id": "s3",
            "title": "阶段3: 需求对齐验证",
            "type": "handoff",
            "target_role": "pm",
            "prompt_template": PROMPT_S3,
            "approval_prompt": APPROVE_S3,
            "failure_patterns": ["需求遗漏", "隐性需求未覆盖", "需求追溯矩阵不完整"],
        },
        {
            "step_id": "s4",
            "title": "阶段4: 开发落地设计",
            "type": "handoff",
            "target_role": "engineer",
            "prompt_template": PROMPT_S4,
            "approval_prompt": APPROVE_S4,
            "failure_patterns": ["架构图缺失", "接口契约不完整", "量化指标不具体", "异常处理缺失"],
        },
        {
            "step_id": "s5",
            "title": "阶段5: 实现一致性检查",
            "type": "review",
            "target_role": "reviewer",
            "prompt_template": PROMPT_S5,
            "approval_prompt": APPROVE_S5,
            "failure_patterns": ["实现与文档不一致", "新增未文档化内容", "差异项未修复"],
        },
    ],
})
reg.close()
print("✅ 模板 WL-09 已创建（完整5阶段工作流）")
